## TASKMASTER – Task & Productivity Management Platform
A modern CRUD (Create, Read, Update, Delete) web application designed to organize tasks, track progress, and support academic or professional workflows.
The system includes authentication, role‑based permissions, and a fully responsive interface.

## 🚀 Features

- **User Authentication:** Secure login and registration with session handling.
- **Admin Panel:** System‑wide statistics, task monitoring, and administrative controls.
- **User Dashboard:** Personalized profile view with user information and activity data.
- **Task Operations:** Create, filter, update, and delete tasks with priority and status indicators.
- **Responsive UI:** Built with a mobile‑first approach for seamless use across all devices.
- **Role Management:** Dedicated views and permissions for regular users and administrators.

## 📁 Project Structure

```text
Prueba_De_Desempeno/
├── public/
│   └── views/            # HTML pages (Login, Tasks, Admin, Profile, etc.)
├── src/
│   ├── assets/           # Icons, logos, and visual resources
│   ├── services/         # JavaScript logic (Auth, CRUD, Guards, API handlers)
│   └── styles/           # Modular CSS files for each component/view
├── db.json               # Local JSON Server database
└── package.json          # Scripts and project dependencies
```

## 🛠️ Setup & Installation

Follow these steps to get the project running locally:

1. Clone the Repository: git clone https://github.com/park03697-blip/prueba-de-desempe-o-JavaScript.git

cd prueba-de-desempe-o-JavaScript

2. Install Dependencies: npm install

3. Run the API (Mock Server): npm run api

4. Run the Development Server: npm run dev